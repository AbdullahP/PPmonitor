FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY config.py .
COPY mock_server/ mock_server/
CMD ["uvicorn", "mock_server.server:app", "--host", "0.0.0.0", "--port", "8099"]
